import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginVertical: 10,
  },
  scrollView: {
    maxHeight: 108,
    marginVertical: 10,
  },
  view: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
});

export default styles;
